% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_cultivar_parameters.R
\name{get_xfile_sec}
\alias{get_xfile_sec}
\title{Extract a Section from DSSAT management tables (file X)}
\usage{
get_xfile_sec(xtables, sec_name)
}
\arguments{
\item{xtables}{A named list DSSAT management tables as data frames.}

\item{sec_name}{Character. The section name or pattern to search for in the names of \code{xtables}.}
}
\value{
The first table (e.g., data frame) whose name matches \code{sec_name}, or \code{NULL} if no match is found.
}
\description{
Retrieves the first table from a DSSAT management table list based on section name.
}
\details{
The function searches the names of \code{xtables} for entries matching \code{sec_name} using \code{grepl}. It returns the first matching table, or \code{NULL} if no match is found.

This is useful for extracting a specific section or table from a list of tables by partial or full name.
}
\examples{
xtables <- list(SUMMARY = data.frame(a = 1:3), DETAILS = data.frame(b = 4:6))
get_xfile_sec(xtables, "SUM")
# Returns the SUMMARY data frame

}
