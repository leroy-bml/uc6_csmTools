% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/identify_exp_design.R
\name{is_year_like}
\alias{is_year_like}
\title{Check if a Vector is Year-Like}
\usage{
is_year_like(
  col,
  range = c(1800, as.numeric(format(Sys.Date(), "\%Y"))),
  future_buffer = 5
)
}
\arguments{
\item{col}{A vector to be tested for year-likeness.}

\item{range}{A numeric vector of length 2 specifying the minimum and maximum plausible years (default: c(1800, current year)).}

\item{future_buffer}{An integer specifying how many years beyond the current year to allow as plausible (default: 5).}
}
\value{
Logical. \code{TRUE} if the vector meets all year-like criteria, \code{FALSE} otherwise.
}
\description{
Determines whether a vector is "year-like" based on several criteria:
\itemize{
\item All values are 4-digit numbers
\item All values are integers
\item There is at least one repeated value
\item All values fall within a plausible year range (default: 1800 to current year plus a future buffer)
}
}
\examples{
is_year_like(c(1999, 2000, 2001, 2001))
is_year_like(c("2010", "2011", "2012", "2012"))
is_year_like(c(1999, 2000, 2001, 2002)) # No repeats, returns FALSE
is_year_like(c(199, 2000, 2001, 2001))  # Not all 4 digits, returns FALSE


}
